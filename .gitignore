# Multi-Bot is a docs-and-examples repo. The tracked product lives under
# example-repo-level/ and example-user-level/ (each with its own ignore rules,
# where applicable). Only ignore local, root-level agent scratch here — keep
# these patterns root-anchored so they never match the example trees.
/.claude/
/.mcp.json
